extend type Query {
  me: User
}

extend type Mutation {
  userCreate(input: NewUser!): User
}

type User @key(fields: "id") {
  id: ID!
  email: String!
  username: String!
}

input NewUser {
  email: String!
  username: String!
}
